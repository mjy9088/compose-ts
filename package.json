{
  "private": true,
  "name": "compose-ts",
  "version": "2.0.1",
  "description": "Type safe version of `compose()` util",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.dist.json && package-json-minifier && cpy README.md LICENSE dist",
    "publish": "npm run build && cd dist && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mjy9088/compose-ts.git"
  },
  "keywords": [
    "compose"
  ],
  "author": "Juyeong Maing<mjy9088@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
